require 'spec_helper'

describe JmlTestRunner do
  it 'has a version number' do
    expect(JmlTestRunner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
